[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[project]
name = "systematic-strategies-backtest"
version = "1.5.1"
description = "Common backtesting engine for QR-INFRA-001 (frozen contract v1.5.1)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "pyarrow>=14.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: live Hyperliquid network validation (QR-DATA-001 D§11.3); skipped by default, run with --run-integration",
    "slow: exercises the real offline QR-SMOKE-001 pipeline (QR-INFRA-002 R§17); not skipped by default, just slower (~15-30s).",
]
