"""QR-SMOKE-001 v1.0 FROZEN — end-to-end pipeline smoke test.

See docs/qr_smoke_001_spec.md. This package contains the harness
(`pipeline.py`), the independent reconstruction (`reconstruction.py`), the
cross-venue analysis (`crossvenue.py`) and the experiment driver
(`run_all.py`). NOT alpha research (spec §0).
"""
